/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['img.freepik.com', 'images.pexels.com', 'via.placeholder.com'], // Allow external images from freepik
  },
};

export default nextConfig;
